export const DEFAULT_SCRIPT_RULES = `
- Include all important details mentioned in the App Description in the demo script.
- The number of segments must exactly match the number of script_lines.
- Each segment's narration in the script_lines should be complete. Don't break sentences between narrations. And never use "..." dotted lines to show continuation.`;

export const DEFAULT_TTS_STYLE = "Read aloud in a clear and confident tone.";

